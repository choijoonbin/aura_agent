fastapi>=0.115.0
uvicorn[standard]>=0.30.0
streamlit>=1.42.0
streamlit-option-menu>=0.3.0
pandas>=2.0.0
plotly>=5.0.0
matplotlib>=3.7.0
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
httpx>=0.27.0
pydantic>=2.8.0
python-dotenv>=1.0.1
openai>=1.40.0
graphviz>=0.20.3
neo4j>=5.22.0

langgraph>=0.2.0
# CHECKPOINTER_BACKEND=postgres 사용 시 별도 설치: pip install langgraph-checkpoint-postgres
langchain-core>=0.3.0
langchain>=0.3.0

# Langfuse (observability, optional)
langfuse>=2.0.0

# Phase F: cross-encoder rerank + 한국어 임베딩 (optional).
# 미설치 시 rerank/임베딩은 스킵되고 BM25 또는 LIKE fallback 사용.
# torch 호환 환경에서만: pip install torch sentence-transformers (또는 requirements-optional.txt)
# sentence-transformers>=2.2,<3.3
# safetensors>=0.4.0

# 증빙 이미지 OCR
Pillow>=10.0.0

# PaddleOCR — 2단계 OCR+LLM 파이프라인 (픽셀 단위 정확한 bbox)
# CPU: pip install paddlepaddle paddleocr
# GPU: pip install paddlepaddle-gpu paddleocr
# 미설치 시 자동으로 Vision LLM 단일 단계로 fallback됨
paddlepaddle>=2.6.0
paddleocr>=2.7.0
